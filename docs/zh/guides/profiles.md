# 浏览器 profile

Profile 允许你跨运行保存和恢复浏览器会话。一个 profile 将 cookie、localStorage 和其他浏览器状态保存在 `~/.agentcloak/profiles/` 下的命名目录中。

本指南将涵盖从头创建 profile 或从运行中的会话创建、使用已保存 profile 启动、管理多个 profile 用于不同账户或站点，以及 profile 和后端之间的 cookie 导出/导入。

详细内容将在后续更新中补充。profile 命令参见 [CLI 参考](../reference/cli.md#profile-管理)。
